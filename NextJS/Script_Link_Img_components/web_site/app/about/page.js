import React from 'react'

const about = () => {
  return (
    <div>
      This is About.
    </div>
  )
}

export default about

export const metadata = {
    title: "About Facebook",
    description: "This is about page for Facebook n we can connect with it. ",
  };
